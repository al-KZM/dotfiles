# colors
source colors/mutt-colors-solarized-dark-256.muttrc



# Eyal@chocron.eu 
set imap_user = eyal@chocron.eu
set imap_pass = cztirkzqlagabwiz
set smtp_pass = cztirkzqlagabwiz

set from = "eyal@chocron.eu"
set realname = "Eyal Shukrun"

set folder = imaps://eyal@chocron.eu@imap.gmail.com/
set smtp_url = "smtp://eyal@chocron.eu@smtp.gmail.com:587"

set spoolfile = +INBOX
set record = +Sent
set trash = +Trash
set postponed = +Drafts

mailboxes =INBOX =Sent =Trash =Drafts 

# Store message headers locally to speed things up
set header_cache = ~/.cache/mutt

# Store messages locally to speed things up, like searching message bodies.
# This will cost important disk usage 
set message_cachedir = "~/.cache/mutt"

# Allow Mutt to open a new IMAP connection automatically.
unset imap_passive

# Keep the IMAP connection alive by polling intermittently (time in seconds).
#set imap_keepalive = 300

# How often to check for new mail (time in seconds).
set mail_check = 300


